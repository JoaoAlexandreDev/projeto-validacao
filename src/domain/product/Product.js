export default class Product {
    constructor(sku, descricao, data_referencia, valor_referencia) {
        this.sku = sku;
        this.descricao = descricao;
        this.data_referencia = data_referencia;
        this.valor_referencia = valor_referencia;
    }
}